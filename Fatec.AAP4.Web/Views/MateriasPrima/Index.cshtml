@model IEnumerable<Fatec.AAP4.Web.Models.materia_prima>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Incluir", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.descricao_matprima)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.descricao_matprima)
        </td>
        <td>
            @Html.ActionLink("Editar", "Edit", new { id=item.id_matprima }) |
            @Html.ActionLink("Detalhes", "Details", new { id=item.id_matprima }) |
            @Html.ActionLink("Deletar", "Delete", new { id=item.id_matprima })
        </td>
    </tr>
}

</table>
