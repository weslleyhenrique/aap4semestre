@model IEnumerable<Fatec.AAP4.Web.Models.plano_contas>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Incluir", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.codigo_planocontas)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.descricao_planocontas)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.dre.nome_dre)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.codigo_planocontas)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.descricao_planocontas)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.dre.nome_dre)
        </td>
        <td>
            @Html.ActionLink("Editar", "Edit", new { id=item.id_planocontas }) |
            @Html.ActionLink("Detalhes", "Details", new { id=item.id_planocontas }) |
            @Html.ActionLink("Deletar", "Delete", new { id=item.id_planocontas })
        </td>
    </tr>
}

</table>
