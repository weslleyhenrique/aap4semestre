@model IEnumerable<Fatec.AAP4.Web.Models.dre>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Incluir", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.nome_dre)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.nome_dre)
        </td>
        <td>
            @Html.ActionLink("Editar", "Edit", new { id=item.id_dre }) |
            @Html.ActionLink("Detalhes", "Details", new { id=item.id_dre }) |
            @Html.ActionLink("Deletar", "Delete", new { id=item.id_dre })
        </td>
    </tr>
}

</table>
